node_modules/
dist/

# bun.lock is the checked-in lockfile; npm's is a local by-product.
package-lock.json
.env
.env.local

# Runtime state: SQLite database, uploaded screenshots, generated dev secret.
server/data/
*.tsbuildinfo
