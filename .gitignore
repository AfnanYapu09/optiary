node_modules/
dist/

# bun.lock is the checked-in lockfile; npm's is a local by-product.
package-lock.json
.env
.env.local

# Runtime state. Research data now lives in Supabase; server/data/ is only the
# leftover local SQLite database kept around for the one-off migration.
server/data/
.session-secret
*.tsbuildinfo
